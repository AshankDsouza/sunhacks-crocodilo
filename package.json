{
  "name": "sunhacks-cocodrilo",
  "version": "0.1.0",
  "private": true,
  "description": "Small chatbot site to track C2O footprint using a Gemini-compatible API via a secure server proxy",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
